# Preview behavior

The embedded file preview may block microphone, GPS, service workers, and IndexedDB.

Use **Run preview demo** to test the interface in preview.

For real recording, run `python server.py` and open `http://localhost:8000`, or deploy the folder to an HTTPS static host and open it on your phone.
