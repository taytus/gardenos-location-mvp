# GardenOS Location v0.4.0

## Changes
- Added a visible app version badge: `v: 0.4.0`.
- Added a single `APP_VERSION` constant in `index.html`.
- Added a root `VERSION` file for release tracking.
- Kept production-only GPS behavior with no preview button.

## Versioning
The assistant controls the application version number from this release onward.

Current version: `0.4.0`
